/**
 * Control center slider helpers (placeholder).
 *
 * Notes:
 * - Refactored to pass robot records into handlers so slider logic stays per robot.
 * - Keep new code following this per-robot pattern.
 * - TODO: move sliders out of `index.js` into this module.
 */